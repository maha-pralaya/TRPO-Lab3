import React from 'react';
import ReactDOM from 'react-dom';
import Sent from './components/App';
// Everything is working, final test

ReactDOM.render(
  <Sent />,
  document.querySelector('#App'),
);
